package editortrees;

public class BalanceCodeChecker {
	boolean isCorrect;
	int height;
	
	public BalanceCodeChecker(boolean isCorrect, int height) {
		this.isCorrect = isCorrect;
		this.height = height;
	}
}
